import java

// Exercise 3: Find all static fields whose name starts with "ERROR".
// Note: some results might be in the JDK or libraries (those sources are not included in the CodeQL database).

from Field f
// TODO: add where clause that solves the exercise
select f
